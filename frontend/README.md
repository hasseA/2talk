# 2talk frontend

Minimal React and TypeScript application powered by Vite.

Install dependencies with `npm install`, start development with `npm run dev`, and create the Playwright browser installation with `npx playwright install` before running `npm run test:e2e`.
